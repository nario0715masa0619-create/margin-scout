{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd margin-scout-backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd margin-scout-backend && uvicorn app.main:app --host 0.0.0.0 --port `$PORT"
  }
}
